package com.paxtech.utime.platform.analytics.domain.model.queries;

/**
 * Query to obtain the total number of recorded events grouped by event type,
 * used to feed the analytics dashboard.
 */
public record GetAnalyticsSummaryQuery() {
}
